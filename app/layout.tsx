import type { Metadata } from "next";
import { Cormorant_Garamond, Jost } from "next/font/google";
import "./globals.css";

const cormorant = Cormorant_Garamond({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600"],
  style: ["normal", "italic"],
  variable: "--font-cormorant",
  display: "swap",
});

const jost = Jost({
  subsets: ["latin"],
  weight: ["300", "400", "500"],
  variable: "--font-jost",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Eden Sassoon",
  description:
    "Beauty entrepreneur, wellness advocate, and philanthropist. Daughter of Vidal Sassoon. Founder of EDEN salon, Pilates studios, and Beauty Gives Back.",
  openGraph: {
    title: "Eden Sassoon",
    description: "Beauty entrepreneur. Wellness advocate. Forever a work in progress.",
    url: "https://edensassoon.com",
    siteName: "Eden Sassoon",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${cormorant.variable} ${jost.variable}`}>
      <body>{children}</body>
    </html>
  );
}
